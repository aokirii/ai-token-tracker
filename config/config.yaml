# Application / display settings (safe to share / commit).
# For user-specific paths, see config/user.config.yaml

window_hours: 5            # rolling window length (Claude Pro plan = 5h)
refresh_seconds: 30        # how often the UI refreshes
live_interval_seconds: 60  # lower bound for live API calls (network hit at most once per this)

providers:
  - id: claude
    name: Claude
    source: claude_auto      # automatic: live API, cache, jsonl
    plan: auto               # 'auto' = detect from credentials; or set a fixed label
    limit: 1000000           # quota for manual providers; unused for claude_auto
    context_limit: auto      # context-window size: 'auto' (reads model for [1m]; else 200k, bumps to 1M when used>200k), or pin 200000 / 1000000
    context_sessions: 3      # max number of open Claude sessions to show separate context bars for (detected from running processes)
    color: "#d97757"

  - id: codex
    name: Codex
    source: codex_auto       # automatic: Codex app-server, local auth/state fallback
    plan: auto               # 'auto' = detect from ~/.codex auth; or set a fixed label
    used: 0
    limit: 100
    color: "#10a37f"

  - id: antigravity
    name: Antigravity
    source: manual
    plan: free               # your Antigravity plan label
    used: 0
    limit: 1000000
    color: "#4285f4"
